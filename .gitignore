# Whitelist-based: ignore EVERYTHING by default, then un-ignore tracked items.
# This ensures any new directory Claude Code adds to ~/.claude/ is automatically ignored.
# To track a new file/dir: add a !pattern below, then `git add` it.

# Ignore everything
*

# ── Un-ignore tracked files ──
!.gitignore
!CLAUDE.md
!LICENSE
!README.md
!install.sh
!uninstall.sh
!settings.json

# ── Un-ignore tracked directories ──
# (must un-ignore dir first, then its contents)
!agents/
!agents/**
!commands/
!commands/**
!container/
!container/**
!hooks/
!hooks/**
!rules/
!rules/**

# ── Still ignore within tracked dirs ──
container/projects/
.DS_Store
